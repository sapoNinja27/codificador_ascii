import java.util.Scanner;

public class CodificadorParidadeASCII {

    public static void main(String[] args) {
        Bidimensional.bidimensional();
        /*
        Scanner scanner = new Scanner(System.in);
        String s = scanner.nextLine();
        if (s.equals("s")) {
            Simples.simples();
        }
        if (s.equals("b")) {
            Bidimensional.bidimensional();
        }
        if (s.equals("p")) {
            Simples.simples();
        }
         */
    }
}